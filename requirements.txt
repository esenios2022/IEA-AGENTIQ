fastapi>=0.110.0
uvicorn[standard]>=0.29.0
# 2026-08-14 -- implementacion de websocket alternativa a la libreria
# "websockets" (que uvicorn[standard] ya trae). Descubierto real: detras del
# proxy de Railway, "websockets" rechaza con 403 CUALQUIER conexion
# websocket real (probado con dos clientes distintos, con y sin Origin
# explicito matcheando el dominio publico) antes de que la app la vea
# siquiera -- confirmado en logs reales de Uvicorn ("connection rejected
# (403 Forbidden)"), no un problema del codigo de la app. wsproto no aplica
# ese chequeo de origen por default (verificado local: conecta incluso con
# un Origin completamente distinto), asi que sortea el problema sin tener
# que armar un allowlist de origenes a mano. Ver Procfile/railway.json
# (--ws wsproto) y src/main.py::agent_chat_socket.
wsproto>=1.2.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0
redis>=5.0.0
pydantic[email]>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
pytest>=8.0.0
httpx>=0.27.0
anthropic>=0.40.0
openai>=1.0.0
jinja2>=3.1.0
python-multipart>=0.0.9
crewai>=0.80.0
google-api-python-client>=2.140.0
google-auth>=2.34.0
google-genai>=1.0.0
bcrypt>=4.2.0
itsdangerous>=2.2.0
composio>=0.17.0
composio-crewai>=0.3.30
apscheduler>=3.10.0
requests>=2.32.0
pypdf>=4.0.0
python-docx>=1.1.0
boto3>=1.34.0
astronomy-engine>=2.1.19
